// Auto-generated by tools/generate.py â€” DO NOT EDIT BY HAND.
#include <Windows.h>
#include <memory>
#include <string>
#include "widget_tree.h"
#include "utf.h"
#include "generated/widgets_clickable.h"

#define API_EXPORT extern "C" __declspec(dllexport)

API_EXPORT int __cdecl ImGui_CreateButton(const wchar_t* id, const wchar_t* label)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    if (uid.empty()) return 1;
    auto widget = std::make_unique<ButtonWidget>();
    widget->id    = uid;
    widget->label = ulbl;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(widget)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateSmallButton(const wchar_t* id, const wchar_t* label)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    if (uid.empty()) return 1;
    auto widget = std::make_unique<SmallButtonWidget>();
    widget->id    = uid;
    widget->label = ulbl;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(widget)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateArrowButton(const wchar_t* id, const wchar_t* label, int dir)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    if (uid.empty()) return 1;
    auto widget = std::make_unique<ArrowButtonWidget>();
    widget->id    = uid;
    widget->label = ulbl;
    widget->dir = dir;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(widget)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateInvisibleButton(const wchar_t* id, const wchar_t* label, float w, float h)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    if (uid.empty()) return 1;
    auto widget = std::make_unique<InvisibleButtonWidget>();
    widget->id    = uid;
    widget->label = ulbl;
    widget->w = w;
    widget->h = h;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(widget)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateTextLink(const wchar_t* id, const wchar_t* label)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    if (uid.empty()) return 1;
    auto widget = std::make_unique<TextLinkWidget>();
    widget->id    = uid;
    widget->label = ulbl;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(widget)) ? 0 : 2;
}

