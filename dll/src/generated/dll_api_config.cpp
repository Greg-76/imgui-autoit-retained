// Auto-generated by tools/generate.py â€” DO NOT EDIT BY HAND.
#include <Windows.h>
#include <memory>
#include <string>
#include "widget_tree.h"
#include "utf.h"
#include "generated/widgets_config.h"

#define API_EXPORT extern "C" __declspec(dllexport)

API_EXPORT int __cdecl ImGui_CreatePushStyleColor(const wchar_t* id, int idx, float r, float g, float b, float a)
{
    if (!id || !*id) return 1;
    std::string uid = WideToUtf8(id);
    if (uid.empty()) return 1;
    auto w = std::make_unique<PushStyleColorWidget>();
    w->id = uid;
    w->idx = idx;
    w->r = r;
    w->g = g;
    w->b = b;
    w->a = a;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreatePopStyleColor(const wchar_t* id, int count)
{
    if (!id || !*id) return 1;
    std::string uid = WideToUtf8(id);
    if (uid.empty()) return 1;
    auto w = std::make_unique<PopStyleColorWidget>();
    w->id = uid;
    w->count = count;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreatePushStyleVarFloat(const wchar_t* id, int idx, float value)
{
    if (!id || !*id) return 1;
    std::string uid = WideToUtf8(id);
    if (uid.empty()) return 1;
    auto w = std::make_unique<PushStyleVarFloatWidget>();
    w->id = uid;
    w->idx = idx;
    w->value = value;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreatePushStyleVarVec2(const wchar_t* id, int idx, float value_x, float value_y)
{
    if (!id || !*id) return 1;
    std::string uid = WideToUtf8(id);
    if (uid.empty()) return 1;
    auto w = std::make_unique<PushStyleVarVec2Widget>();
    w->id = uid;
    w->idx = idx;
    w->value_x = value_x;
    w->value_y = value_y;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreatePopStyleVar(const wchar_t* id, int count)
{
    if (!id || !*id) return 1;
    std::string uid = WideToUtf8(id);
    if (uid.empty()) return 1;
    auto w = std::make_unique<PopStyleVarWidget>();
    w->id = uid;
    w->count = count;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreatePushStyleVarX(const wchar_t* id, int idx, float value_x)
{
    if (!id || !*id) return 1;
    std::string uid = WideToUtf8(id);
    if (uid.empty()) return 1;
    auto w = std::make_unique<PushStyleVarXWidget>();
    w->id = uid;
    w->idx = idx;
    w->value_x = value_x;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreatePushStyleVarY(const wchar_t* id, int idx, float value_y)
{
    if (!id || !*id) return 1;
    std::string uid = WideToUtf8(id);
    if (uid.empty()) return 1;
    auto w = std::make_unique<PushStyleVarYWidget>();
    w->id = uid;
    w->idx = idx;
    w->value_y = value_y;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreatePushItemWidth(const wchar_t* id, float item_width)
{
    if (!id || !*id) return 1;
    std::string uid = WideToUtf8(id);
    if (uid.empty()) return 1;
    auto w = std::make_unique<PushItemWidthWidget>();
    w->id = uid;
    w->item_width = item_width;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreatePopItemWidth(const wchar_t* id)
{
    if (!id || !*id) return 1;
    std::string uid = WideToUtf8(id);
    if (uid.empty()) return 1;
    auto w = std::make_unique<PopItemWidthWidget>();
    w->id = uid;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreatePushTextWrapPos(const wchar_t* id, float wrap_local_pos_x)
{
    if (!id || !*id) return 1;
    std::string uid = WideToUtf8(id);
    if (uid.empty()) return 1;
    auto w = std::make_unique<PushTextWrapPosWidget>();
    w->id = uid;
    w->wrap_local_pos_x = wrap_local_pos_x;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreatePopTextWrapPos(const wchar_t* id)
{
    if (!id || !*id) return 1;
    std::string uid = WideToUtf8(id);
    if (uid.empty()) return 1;
    auto w = std::make_unique<PopTextWrapPosWidget>();
    w->id = uid;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreatePushItemFlag(const wchar_t* id, int option, int enabled)
{
    if (!id || !*id) return 1;
    std::string uid = WideToUtf8(id);
    if (uid.empty()) return 1;
    auto w = std::make_unique<PushItemFlagWidget>();
    w->id = uid;
    w->option = option;
    w->enabled = enabled;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreatePopItemFlag(const wchar_t* id)
{
    if (!id || !*id) return 1;
    std::string uid = WideToUtf8(id);
    if (uid.empty()) return 1;
    auto w = std::make_unique<PopItemFlagWidget>();
    w->id = uid;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreatePushClipRect(const wchar_t* id, float min_x, float min_y, float max_x, float max_y, int intersect)
{
    if (!id || !*id) return 1;
    std::string uid = WideToUtf8(id);
    if (uid.empty()) return 1;
    auto w = std::make_unique<PushClipRectWidget>();
    w->id = uid;
    w->min_x = min_x;
    w->min_y = min_y;
    w->max_x = max_x;
    w->max_y = max_y;
    w->intersect = intersect;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreatePopClipRect(const wchar_t* id)
{
    if (!id || !*id) return 1;
    std::string uid = WideToUtf8(id);
    if (uid.empty()) return 1;
    auto w = std::make_unique<PopClipRectWidget>();
    w->id = uid;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

