// Auto-generated by tools/generate.py â€” DO NOT EDIT BY HAND.
#include <Windows.h>
#include <memory>
#include <string>
#include "widget_tree.h"
#include "utf.h"
#include "generated/widgets_container.h"

#define API_EXPORT extern "C" __declspec(dllexport)

API_EXPORT int __cdecl ImGui_CreateTabBar(const wchar_t* id, const wchar_t* label, int flags)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    if (uid.empty()) return 1;
    auto widget = std::make_unique<TabBarWidget>();
    widget->id    = uid;
    widget->label = ulbl;
    widget->flags = flags;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(widget)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateGroup(const wchar_t* id, const wchar_t* label)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    if (uid.empty()) return 1;
    auto widget = std::make_unique<GroupWidget>();
    widget->id    = uid;
    widget->label = ulbl;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(widget)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateMenuBar(const wchar_t* id, const wchar_t* label)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    if (uid.empty()) return 1;
    auto widget = std::make_unique<MenuBarWidget>();
    widget->id    = uid;
    widget->label = ulbl;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(widget)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateMainMenuBar(const wchar_t* id, const wchar_t* label)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    if (uid.empty()) return 1;
    auto widget = std::make_unique<MainMenuBarWidget>();
    widget->id    = uid;
    widget->label = ulbl;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(widget)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateMenu(const wchar_t* id, const wchar_t* label)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    if (uid.empty()) return 1;
    auto widget = std::make_unique<MenuWidget>();
    widget->id    = uid;
    widget->label = ulbl;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(widget)) ? 0 : 2;
}

