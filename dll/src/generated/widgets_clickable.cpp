// Auto-generated by tools/generate.py â€” DO NOT EDIT BY HAND.
#include "generated/widgets_clickable.h"
#include "imgui.h"

void ButtonWidget::Render()
{
    if (!visible) return;
    if (!enabled) ImGui::BeginDisabled();
    // PushID with the user's stable key so duplicate labels remain distinct.
    ImGui::PushID(id.c_str());
    const char* shown = label.empty() ? id.c_str() : label.c_str();
    if (ImGui::Button(shown)) {
        clicked = true;
    }
    ImGui::PopID();
    if (!enabled) ImGui::EndDisabled();
}

void SmallButtonWidget::Render()
{
    if (!visible) return;
    if (!enabled) ImGui::BeginDisabled();
    // PushID with the user's stable key so duplicate labels remain distinct.
    ImGui::PushID(id.c_str());
    const char* shown = label.empty() ? id.c_str() : label.c_str();
    if (ImGui::SmallButton(shown)) {
        clicked = true;
    }
    ImGui::PopID();
    if (!enabled) ImGui::EndDisabled();
}

void ArrowButtonWidget::Render()
{
    if (!visible) return;
    if (!enabled) ImGui::BeginDisabled();
    // PushID with the user's stable key so duplicate labels remain distinct.
    ImGui::PushID(id.c_str());
    const char* shown = label.empty() ? id.c_str() : label.c_str();
    if (ImGui::ArrowButton(shown, (ImGuiDir)dir)) {
        clicked = true;
    }
    ImGui::PopID();
    if (!enabled) ImGui::EndDisabled();
}

void InvisibleButtonWidget::Render()
{
    if (!visible) return;
    if (!enabled) ImGui::BeginDisabled();
    // PushID with the user's stable key so duplicate labels remain distinct.
    ImGui::PushID(id.c_str());
    const char* shown = label.empty() ? id.c_str() : label.c_str();
    if (ImGui::InvisibleButton(shown, ImVec2(w, h))) {
        clicked = true;
    }
    ImGui::PopID();
    if (!enabled) ImGui::EndDisabled();
}

void TextLinkWidget::Render()
{
    if (!visible) return;
    if (!enabled) ImGui::BeginDisabled();
    // PushID with the user's stable key so duplicate labels remain distinct.
    ImGui::PushID(id.c_str());
    const char* shown = label.empty() ? id.c_str() : label.c_str();
    if (ImGui::TextLink(shown)) {
        clicked = true;
    }
    ImGui::PopID();
    if (!enabled) ImGui::EndDisabled();
}

