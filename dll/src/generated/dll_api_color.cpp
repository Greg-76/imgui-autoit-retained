// Auto-generated by tools/generate.py â€” DO NOT EDIT BY HAND.
#include <Windows.h>
#include <memory>
#include <string>
#include "widget_tree.h"
#include "utf.h"
#include "generated/widgets_color.h"

#define API_EXPORT extern "C" __declspec(dllexport)

API_EXPORT int __cdecl ImGui_CreateColorEdit3(const wchar_t* id, const wchar_t* label,
                                float default_0, float default_1, float default_2, int flags)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    if (uid.empty()) return 1;
    auto w = std::make_unique<ColorEdit3Widget>();
    w->id = uid; w->label = ulbl;
    w->values[0] = default_0;
    w->values[1] = default_1;
    w->values[2] = default_2;
    w->flags = flags;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateColorEdit4(const wchar_t* id, const wchar_t* label,
                                float default_0, float default_1, float default_2, float default_3, int flags)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    if (uid.empty()) return 1;
    auto w = std::make_unique<ColorEdit4Widget>();
    w->id = uid; w->label = ulbl;
    w->values[0] = default_0;
    w->values[1] = default_1;
    w->values[2] = default_2;
    w->values[3] = default_3;
    w->flags = flags;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateColorPicker3(const wchar_t* id, const wchar_t* label,
                                float default_0, float default_1, float default_2, int flags)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    if (uid.empty()) return 1;
    auto w = std::make_unique<ColorPicker3Widget>();
    w->id = uid; w->label = ulbl;
    w->values[0] = default_0;
    w->values[1] = default_1;
    w->values[2] = default_2;
    w->flags = flags;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateColorPicker4(const wchar_t* id, const wchar_t* label,
                                float default_0, float default_1, float default_2, float default_3, int flags)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    if (uid.empty()) return 1;
    auto w = std::make_unique<ColorPicker4Widget>();
    w->id = uid; w->label = ulbl;
    w->values[0] = default_0;
    w->values[1] = default_1;
    w->values[2] = default_2;
    w->values[3] = default_3;
    w->flags = flags;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

