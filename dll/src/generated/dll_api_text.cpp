// Auto-generated by tools/generate.py â€” DO NOT EDIT BY HAND.
#include <Windows.h>
#include <memory>
#include <string>
#include "widget_tree.h"
#include "utf.h"
#include "generated/widgets_text.h"

#define API_EXPORT extern "C" __declspec(dllexport)

API_EXPORT int __cdecl ImGui_CreateTextColored(const wchar_t* id, const wchar_t* text, float r, float g, float b, float a)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string utxt = WideToUtf8(text ? text : L"");
    if (uid.empty()) return 1;
    auto w = std::make_unique<TextColoredWidget>();
    w->id = uid; w->label = utxt;
    w->r = r; w->g = g; w->b = b; w->a = a;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateTextWrapped(const wchar_t* id, const wchar_t* text)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string utxt = WideToUtf8(text ? text : L"");
    if (uid.empty()) return 1;
    auto w = std::make_unique<TextWrappedWidget>();
    w->id = uid; w->label = utxt;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateTextDisabled(const wchar_t* id, const wchar_t* text)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string utxt = WideToUtf8(text ? text : L"");
    if (uid.empty()) return 1;
    auto w = std::make_unique<TextDisabledWidget>();
    w->id = uid; w->label = utxt;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateBulletText(const wchar_t* id, const wchar_t* text)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string utxt = WideToUtf8(text ? text : L"");
    if (uid.empty()) return 1;
    auto w = std::make_unique<BulletTextWidget>();
    w->id = uid; w->label = utxt;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateSeparatorText(const wchar_t* id, const wchar_t* text)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string utxt = WideToUtf8(text ? text : L"");
    if (uid.empty()) return 1;
    auto w = std::make_unique<SeparatorTextWidget>();
    w->id = uid; w->label = utxt;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateLabelText(const wchar_t* id, const wchar_t* text, const wchar_t* extra_key)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string utxt = WideToUtf8(text ? text : L"");
    if (uid.empty()) return 1;
    auto w = std::make_unique<LabelTextWidget>();
    w->id = uid; w->label = utxt;
    w->extra = WideToUtf8(extra_key ? extra_key : L"");
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

