// Auto-generated by tools/generate.py â€” DO NOT EDIT BY HAND.
#include <Windows.h>
#include <memory>
#include <string>
#include "widget_tree.h"
#include "utf.h"
#include "generated/widgets_value_numeric.h"

#define API_EXPORT extern "C" __declspec(dllexport)

API_EXPORT int __cdecl ImGui_CreateSliderFloat(const wchar_t* id, const wchar_t* label,
                                float v_min, float v_max, float default_value,
                                const wchar_t* format)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    std::string ufmt = WideToUtf8(format && *format ? format : L"%.3f");
    if (uid.empty()) return 1;
    auto w = std::make_unique<SliderFloatWidget>();
    w->id = uid; w->label = ulbl;
    w->v_min = v_min; w->v_max = v_max; w->value = default_value;
    w->format = ufmt;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateSliderInt(const wchar_t* id, const wchar_t* label,
                                int v_min, int v_max, int default_value,
                                const wchar_t* format)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    std::string ufmt = WideToUtf8(format && *format ? format : L"%d");
    if (uid.empty()) return 1;
    auto w = std::make_unique<SliderIntWidget>();
    w->id = uid; w->label = ulbl;
    w->v_min = v_min; w->v_max = v_max; w->value = default_value;
    w->format = ufmt;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateSliderFloat2(const wchar_t* id, const wchar_t* label,
                                float v_min, float v_max,
                                float default_0, float default_1,
                                const wchar_t* format)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    std::string ufmt = WideToUtf8(format && *format ? format : L"%.3f");
    if (uid.empty()) return 1;
    auto w = std::make_unique<SliderFloat2Widget>();
    w->id = uid; w->label = ulbl;
    w->v_min = v_min; w->v_max = v_max;
    w->values[0] = default_0;
    w->values[1] = default_1;
    w->format = ufmt;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateSliderFloat3(const wchar_t* id, const wchar_t* label,
                                float v_min, float v_max,
                                float default_0, float default_1, float default_2,
                                const wchar_t* format)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    std::string ufmt = WideToUtf8(format && *format ? format : L"%.3f");
    if (uid.empty()) return 1;
    auto w = std::make_unique<SliderFloat3Widget>();
    w->id = uid; w->label = ulbl;
    w->v_min = v_min; w->v_max = v_max;
    w->values[0] = default_0;
    w->values[1] = default_1;
    w->values[2] = default_2;
    w->format = ufmt;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateSliderFloat4(const wchar_t* id, const wchar_t* label,
                                float v_min, float v_max,
                                float default_0, float default_1, float default_2, float default_3,
                                const wchar_t* format)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    std::string ufmt = WideToUtf8(format && *format ? format : L"%.3f");
    if (uid.empty()) return 1;
    auto w = std::make_unique<SliderFloat4Widget>();
    w->id = uid; w->label = ulbl;
    w->v_min = v_min; w->v_max = v_max;
    w->values[0] = default_0;
    w->values[1] = default_1;
    w->values[2] = default_2;
    w->values[3] = default_3;
    w->format = ufmt;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateSliderInt2(const wchar_t* id, const wchar_t* label,
                                int v_min, int v_max,
                                int default_0, int default_1,
                                const wchar_t* format)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    std::string ufmt = WideToUtf8(format && *format ? format : L"%d");
    if (uid.empty()) return 1;
    auto w = std::make_unique<SliderInt2Widget>();
    w->id = uid; w->label = ulbl;
    w->v_min = v_min; w->v_max = v_max;
    w->values[0] = default_0;
    w->values[1] = default_1;
    w->format = ufmt;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateSliderInt3(const wchar_t* id, const wchar_t* label,
                                int v_min, int v_max,
                                int default_0, int default_1, int default_2,
                                const wchar_t* format)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    std::string ufmt = WideToUtf8(format && *format ? format : L"%d");
    if (uid.empty()) return 1;
    auto w = std::make_unique<SliderInt3Widget>();
    w->id = uid; w->label = ulbl;
    w->v_min = v_min; w->v_max = v_max;
    w->values[0] = default_0;
    w->values[1] = default_1;
    w->values[2] = default_2;
    w->format = ufmt;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateSliderInt4(const wchar_t* id, const wchar_t* label,
                                int v_min, int v_max,
                                int default_0, int default_1, int default_2, int default_3,
                                const wchar_t* format)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    std::string ufmt = WideToUtf8(format && *format ? format : L"%d");
    if (uid.empty()) return 1;
    auto w = std::make_unique<SliderInt4Widget>();
    w->id = uid; w->label = ulbl;
    w->v_min = v_min; w->v_max = v_max;
    w->values[0] = default_0;
    w->values[1] = default_1;
    w->values[2] = default_2;
    w->values[3] = default_3;
    w->format = ufmt;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateDragFloat(const wchar_t* id, const wchar_t* label,
                                float v_speed, float v_min, float v_max,
                                float default_value, const wchar_t* format)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    std::string ufmt = WideToUtf8(format && *format ? format : L"%.3f");
    if (uid.empty()) return 1;
    auto w = std::make_unique<DragFloatWidget>();
    w->id = uid; w->label = ulbl;
    w->v_speed = v_speed;
    w->v_min = v_min; w->v_max = v_max; w->value = default_value;
    w->format = ufmt;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateDragInt(const wchar_t* id, const wchar_t* label,
                                float v_speed, int v_min, int v_max,
                                int default_value, const wchar_t* format)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    std::string ufmt = WideToUtf8(format && *format ? format : L"%d");
    if (uid.empty()) return 1;
    auto w = std::make_unique<DragIntWidget>();
    w->id = uid; w->label = ulbl;
    w->v_speed = v_speed;
    w->v_min = v_min; w->v_max = v_max; w->value = default_value;
    w->format = ufmt;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateDragFloat2(const wchar_t* id, const wchar_t* label,
                                float v_speed, float v_min, float v_max,
                                float default_0, float default_1,
                                const wchar_t* format)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    std::string ufmt = WideToUtf8(format && *format ? format : L"%.3f");
    if (uid.empty()) return 1;
    auto w = std::make_unique<DragFloat2Widget>();
    w->id = uid; w->label = ulbl;
    w->v_speed = v_speed;
    w->v_min = v_min; w->v_max = v_max;
    w->values[0] = default_0;
    w->values[1] = default_1;
    w->format = ufmt;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateDragFloat3(const wchar_t* id, const wchar_t* label,
                                float v_speed, float v_min, float v_max,
                                float default_0, float default_1, float default_2,
                                const wchar_t* format)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    std::string ufmt = WideToUtf8(format && *format ? format : L"%.3f");
    if (uid.empty()) return 1;
    auto w = std::make_unique<DragFloat3Widget>();
    w->id = uid; w->label = ulbl;
    w->v_speed = v_speed;
    w->v_min = v_min; w->v_max = v_max;
    w->values[0] = default_0;
    w->values[1] = default_1;
    w->values[2] = default_2;
    w->format = ufmt;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateDragFloat4(const wchar_t* id, const wchar_t* label,
                                float v_speed, float v_min, float v_max,
                                float default_0, float default_1, float default_2, float default_3,
                                const wchar_t* format)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    std::string ufmt = WideToUtf8(format && *format ? format : L"%.3f");
    if (uid.empty()) return 1;
    auto w = std::make_unique<DragFloat4Widget>();
    w->id = uid; w->label = ulbl;
    w->v_speed = v_speed;
    w->v_min = v_min; w->v_max = v_max;
    w->values[0] = default_0;
    w->values[1] = default_1;
    w->values[2] = default_2;
    w->values[3] = default_3;
    w->format = ufmt;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateDragInt2(const wchar_t* id, const wchar_t* label,
                                float v_speed, int v_min, int v_max,
                                int default_0, int default_1,
                                const wchar_t* format)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    std::string ufmt = WideToUtf8(format && *format ? format : L"%d");
    if (uid.empty()) return 1;
    auto w = std::make_unique<DragInt2Widget>();
    w->id = uid; w->label = ulbl;
    w->v_speed = v_speed;
    w->v_min = v_min; w->v_max = v_max;
    w->values[0] = default_0;
    w->values[1] = default_1;
    w->format = ufmt;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateDragInt3(const wchar_t* id, const wchar_t* label,
                                float v_speed, int v_min, int v_max,
                                int default_0, int default_1, int default_2,
                                const wchar_t* format)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    std::string ufmt = WideToUtf8(format && *format ? format : L"%d");
    if (uid.empty()) return 1;
    auto w = std::make_unique<DragInt3Widget>();
    w->id = uid; w->label = ulbl;
    w->v_speed = v_speed;
    w->v_min = v_min; w->v_max = v_max;
    w->values[0] = default_0;
    w->values[1] = default_1;
    w->values[2] = default_2;
    w->format = ufmt;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateDragInt4(const wchar_t* id, const wchar_t* label,
                                float v_speed, int v_min, int v_max,
                                int default_0, int default_1, int default_2, int default_3,
                                const wchar_t* format)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    std::string ufmt = WideToUtf8(format && *format ? format : L"%d");
    if (uid.empty()) return 1;
    auto w = std::make_unique<DragInt4Widget>();
    w->id = uid; w->label = ulbl;
    w->v_speed = v_speed;
    w->v_min = v_min; w->v_max = v_max;
    w->values[0] = default_0;
    w->values[1] = default_1;
    w->values[2] = default_2;
    w->values[3] = default_3;
    w->format = ufmt;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateInputFloat(const wchar_t* id, const wchar_t* label,
                                              float default_value, float step,
                                              float step_fast, const wchar_t* format)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    std::string ufmt = WideToUtf8(format && *format ? format : L"%.3f");
    if (uid.empty()) return 1;
    auto w = std::make_unique<InputFloatWidget>();
    w->id = uid; w->label = ulbl;
    w->value = default_value;
    w->step = step; w->step_fast = step_fast;
    w->format = ufmt;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateInputInt(const wchar_t* id, const wchar_t* label,
                                            int default_value, int step, int step_fast)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    if (uid.empty()) return 1;
    auto w = std::make_unique<InputIntWidget>();
    w->id = uid; w->label = ulbl;
    w->value = default_value;
    w->step = step; w->step_fast = step_fast;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateInputFloat2(const wchar_t* id, const wchar_t* label,
                                float default_0, float default_1,
                                const wchar_t* format)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    std::string ufmt = WideToUtf8(format && *format ? format : L"%.3f");
    if (uid.empty()) return 1;
    auto w = std::make_unique<InputFloat2Widget>();
    w->id = uid; w->label = ulbl;
    w->values[0] = default_0;
    w->values[1] = default_1;
    w->format = ufmt;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateInputFloat3(const wchar_t* id, const wchar_t* label,
                                float default_0, float default_1, float default_2,
                                const wchar_t* format)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    std::string ufmt = WideToUtf8(format && *format ? format : L"%.3f");
    if (uid.empty()) return 1;
    auto w = std::make_unique<InputFloat3Widget>();
    w->id = uid; w->label = ulbl;
    w->values[0] = default_0;
    w->values[1] = default_1;
    w->values[2] = default_2;
    w->format = ufmt;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateInputFloat4(const wchar_t* id, const wchar_t* label,
                                float default_0, float default_1, float default_2, float default_3,
                                const wchar_t* format)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    std::string ufmt = WideToUtf8(format && *format ? format : L"%.3f");
    if (uid.empty()) return 1;
    auto w = std::make_unique<InputFloat4Widget>();
    w->id = uid; w->label = ulbl;
    w->values[0] = default_0;
    w->values[1] = default_1;
    w->values[2] = default_2;
    w->values[3] = default_3;
    w->format = ufmt;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateInputInt2(const wchar_t* id, const wchar_t* label,
                                int default_0, int default_1)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    if (uid.empty()) return 1;
    auto w = std::make_unique<InputInt2Widget>();
    w->id = uid; w->label = ulbl;
    w->values[0] = default_0;
    w->values[1] = default_1;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateInputInt3(const wchar_t* id, const wchar_t* label,
                                int default_0, int default_1, int default_2)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    if (uid.empty()) return 1;
    auto w = std::make_unique<InputInt3Widget>();
    w->id = uid; w->label = ulbl;
    w->values[0] = default_0;
    w->values[1] = default_1;
    w->values[2] = default_2;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

API_EXPORT int __cdecl ImGui_CreateInputInt4(const wchar_t* id, const wchar_t* label,
                                int default_0, int default_1, int default_2, int default_3)
{
    if (!id || !*id) return 1;
    std::string uid  = WideToUtf8(id);
    std::string ulbl = WideToUtf8(label ? label : L"");
    if (uid.empty()) return 1;
    auto w = std::make_unique<InputInt4Widget>();
    w->id = uid; w->label = ulbl;
    w->values[0] = default_0;
    w->values[1] = default_1;
    w->values[2] = default_2;
    w->values[3] = default_3;
    std::lock_guard<std::recursive_mutex> lk(g_tree.mtx);
    return g_tree.Add(std::move(w)) ? 0 : 2;
}

