// Auto-generated by tools/generate.py â€” DO NOT EDIT BY HAND.
#include "generated/widgets_color.h"
#include "imgui.h"

void ColorEdit3Widget::Render()
{
    if (!visible) return;
    if (!enabled) ImGui::BeginDisabled();
    ImGui::PushID(id.c_str());
    const char* shown = label.empty() ? id.c_str() : label.c_str();
    if (ImGui::ColorEdit3(shown, values, static_cast<ImGuiColorEditFlags>(flags))) {
        changed = true;
    }
    ImGui::PopID();
    if (!enabled) ImGui::EndDisabled();
}

void ColorEdit4Widget::Render()
{
    if (!visible) return;
    if (!enabled) ImGui::BeginDisabled();
    ImGui::PushID(id.c_str());
    const char* shown = label.empty() ? id.c_str() : label.c_str();
    if (ImGui::ColorEdit4(shown, values, static_cast<ImGuiColorEditFlags>(flags))) {
        changed = true;
    }
    ImGui::PopID();
    if (!enabled) ImGui::EndDisabled();
}

void ColorPicker3Widget::Render()
{
    if (!visible) return;
    if (!enabled) ImGui::BeginDisabled();
    ImGui::PushID(id.c_str());
    const char* shown = label.empty() ? id.c_str() : label.c_str();
    if (ImGui::ColorPicker3(shown, values, static_cast<ImGuiColorEditFlags>(flags))) {
        changed = true;
    }
    ImGui::PopID();
    if (!enabled) ImGui::EndDisabled();
}

void ColorPicker4Widget::Render()
{
    if (!visible) return;
    if (!enabled) ImGui::BeginDisabled();
    ImGui::PushID(id.c_str());
    const char* shown = label.empty() ? id.c_str() : label.c_str();
    if (ImGui::ColorPicker4(shown, values, static_cast<ImGuiColorEditFlags>(flags))) {
        changed = true;
    }
    ImGui::PopID();
    if (!enabled) ImGui::EndDisabled();
}

